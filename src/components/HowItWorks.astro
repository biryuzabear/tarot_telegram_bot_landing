---
const steps = [
  {
    number: "1",
    title: "Задайте вопрос",
    description: "Напишите текстом или отправьте голосовое сообщение. Бот понимает любые вопросы о жизни, отношениях, карьере.",
    icon: "💭",
  },
  {
    number: "2",
    title: "Получите расклад",
    description: "Бот выберет карты и создаст красивую визуализацию расклада. Можно выбрать тип расклада: от простого на 3 карты до Кельтского креста.",
    icon: "🎴",
  },
  {
    number: "3",
    title: "Читайте интерпретацию",
    description: "Получите глубокий персонализированный ответ с учётом вашего вопроса, позиций карт и их взаимосвязей.",
    icon: "✨",
  },
];
---

<section id="how-it-works" class="py-24 relative">
  <!-- Background -->
  <div class="absolute inset-0 bg-gradient-to-b from-dark-950 via-dark-900/50 to-dark-950"></div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section header -->
    <div class="text-center mb-16">
      <span class="inline-block px-4 py-1.5 bg-primary-500/10 border border-primary-500/20 rounded-full text-primary-300 text-sm font-medium mb-4">
        Как это работает
      </span>
      <h2 class="font-display text-3xl sm:text-4xl md:text-5xl font-bold text-white mb-4">
        Три простых шага
      </h2>
      <p class="text-dark-300 text-lg max-w-2xl mx-auto">
        От вопроса до инсайта за минуту
      </p>
    </div>

    <!-- Steps -->
    <div class="grid md:grid-cols-3 gap-8 lg:gap-12">
      {steps.map((step, index) => (
        <div class="relative group">
          {/* Connector line */}
          {index < steps.length - 1 && (
            <div class="hidden md:block absolute top-16 left-full w-full h-0.5 bg-gradient-to-r from-primary-500/50 to-transparent -translate-x-1/2 z-0"></div>
          )}

          <div class="relative bg-dark-800/50 border border-white/5 rounded-2xl p-8 hover:border-primary-500/30 transition-all hover:bg-dark-800/80 group-hover:shadow-xl group-hover:shadow-primary-500/5 h-full flex flex-col">
            {/* Number badge */}
            <div class="absolute -top-4 left-8 px-3 py-1 bg-primary-600 rounded-full text-sm font-bold">
              {step.number}
            </div>

            {/* Icon */}
            <div class="text-5xl mb-6 mt-2">
              {step.icon}
            </div>

            {/* Content */}
            <h3 class="font-display text-xl font-semibold text-white mb-3">
              {step.title}
            </h3>
            <p class="text-dark-300 leading-relaxed">
              {step.description}
            </p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
